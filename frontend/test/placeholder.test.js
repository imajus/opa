import { describe, it } from 'mocha';
import { expect } from 'chai';
describe('Project Scaffolding', function() {
  it('should have basic project structure in place', function() {
    expect(true).to.be.true;
  });
});
